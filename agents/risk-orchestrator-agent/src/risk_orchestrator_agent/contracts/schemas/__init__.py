"""JSON-schema references for contract validation.

Empty in this phase by design. This codebase's schemas of record live in
the repository-wide `contracts/agent-contracts/v1/*.schema.json` and
`contracts/events/v1/*.schema.json` files (Phase 1, FRS §6) — this
package is reserved as the future attachment point for any
agent-local schema helpers, without duplicating the repository-wide
schema files themselves.
"""
